package movable;

public interface Movable {
    void move();//в интерфейсе нельзя реализовывать метод {тело} слайд 15 лекция 8
    boolean DFG=false; // переменная по умолчанию публичная и статичная и финальная константа, подсвечивается курсивом
    /* многострочный комментарий
    м ртт мм
     */
}
