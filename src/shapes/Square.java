package shapes;

public class Square extends Rectangle {
    public Square(int numberofsides, int coordinates, int proportion) {
        super(numberofsides, coordinates, proportion);
    }

    @Override
    void draw() {
        super.draw();
    }

}
